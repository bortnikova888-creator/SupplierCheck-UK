# SupplierCheck UK - Environment Configuration
# Copy this file to .env and fill in the required values

# =============================================================================
# SERVER CONFIGURATION (Required for backend)
# =============================================================================

# Companies House API Key (REQUIRED)
# Get your API key at: https://developer.company-information.service.gov.uk/
# This is a server-side only secret - NEVER expose in client code
COMPANIES_HOUSE_API_KEY=your_api_key_here

# Server host and port
HOST=0.0.0.0
PORT=3001

# Logging level: debug | info | warn | error
LOG_LEVEL=info

# Rate limiting configuration
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000

# Database (future use)
DATABASE_URL=

# =============================================================================
# WEB CONFIGURATION (Safe for client-side)
# =============================================================================

# Backend API URL (used by the web client)
VITE_API_URL=http://localhost:3001

# Enable mock mode for development (bypasses real API calls)
VITE_ENABLE_MOCK_MODE=false

# =============================================================================
# SHARED CONFIGURATION
# =============================================================================

# Environment: development | production | test
NODE_ENV=development
