{
  "name": "tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "pnpm --workspace-root playwright:install",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts",
    "build": "echo 'No build step for tests'",
    "clean": "rm -rf coverage"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.2",
    "@pkg/core": "workspace:*",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "@types/supertest": "^2.0.16",
    "@pkg/config": "workspace:*",
    "@types/node": "^22.14.0",
    "nock": "^13.5.6",
    "node-fetch": "^3.3.2",
    "supertest": "^7.0.0",
    "typescript": "~5.8.2",
    "vitest": "^3.0.0"
  }
}
